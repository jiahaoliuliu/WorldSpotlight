<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.worldspotlightapp.android.ui.LoginActivity"
    android:background="@drawable/login_background"
    android:orientation="vertical"
    android:gravity="center"
    >

    <!-- Logo -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/login_logo"
        android:layout_marginBottom="@dimen/login_activity_group_margin_vertical"
        />

    <!-- Facebook Log in button -->
    <Button
        android:id="@+id/facebook_login_button"
        style="@style/FacebookLoginButton"
        android:layout_centerInParent="true"
        android:layout_marginTop="@dimen/login_activity_group_margin_vertical"
        android:layout_marginBottom="@dimen/login_activity_element_margin_vertical"
        />

    <!-- Google Plus Log in button -->
    <Button
        android:id="@+id/google_plus_login_buttn"
        style="@style/GooglePlusLoginButton"
        android:layout_marginTop="@dimen/login_activity_element_margin_vertical"
        android:layout_marginBottom="@dimen/login_activity_separator_margin_vertical"
        />

    <!-- Separation line -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1sp"
        android:background="@android:color/white"
        android:layout_marginTop="@dimen/login_activity_separator_margin_vertical"
        android:layout_marginBottom="@dimen/login_activity_separator_margin_vertical"
        />

    <!-- Skip image -->
    <Button
        android:id="@+id/cancel_image_view"
        style="@style/SkipButton"
        android:layout_marginTop="@dimen/login_activity_separator_margin_vertical"
        android:layout_marginBottom="@dimen/login_activity_element_margin_vertical"
        />
</LinearLayout>
